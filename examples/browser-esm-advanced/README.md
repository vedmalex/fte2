# Browser ESM advanced example

- Shows sync, async, and stream modes with chunked output and deindent.
- Uses `globalThis.fte` registration, `toWebReadable`, and templates from dist.

Open `index.html` via a dev server.
