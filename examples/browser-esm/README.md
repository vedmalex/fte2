# Browser ESM examples

Open these files in a dev server:

- `globalthis.html`: registers templates via `globalThis.fte`
- `cdn.html`: loads from CDN
- `stream.html`: demonstrates stream mode, `toWebReadable`, and cancel
