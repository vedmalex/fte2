# Node ESM example

- Build and run:

```bash
./build-and-run.sh
```

- Demonstrates:
  - CLI bundling (ESM singlefile) with sourcemaps
  - Runtime `run` (sync)
  - How to switch to async/stream in ESM
